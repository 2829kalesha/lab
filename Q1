#include<stdio.h>
void result_binary(int num) {
if(num ==0) {
printf("0");
return;
}
for (int i=31;i>=0;i--) {
int bit =(num >>i) & 1;
printf("%d",bit);
}

}
int main() {
int num;
printf("Entre a number:");
scanf("%d",&num);
result_binary(num);
return 0;
} 
